# Default values for pod-evict.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: transwarp/{{ TOS_IMAGE_REPO }}/antrea
  tag: {{ IMG_VERSION }}
  pullPolicy: IfNotPresent

serviceCIDR: {{ .SERVICECIDR }}
