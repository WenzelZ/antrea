# Default values for pod-evict.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: transwarp/antrea
  tag: {{ IMG_VERSION }}
  pullPolicy: IfNotPresent

serviceCIDR: {{ .SERVICECIDR }}
